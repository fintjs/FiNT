class Fint {
    private token;

    constructor(
        token: String,
        options?: {}
    ) {
        this.token = token;
    }

    private call(method: String, payload: {}) {
        // TODO: Fetch Telegram Bot API
    }
}